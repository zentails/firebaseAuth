<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.firebaseauth">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ShowProfile" />
        <activity android:name=".CreateUser" />
        <activity android:name=".EditProfile" />

        <service
            android:name=".FirebaseBackgroundService"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.example.user.firebaseauth.FirebaseBackgroundService" />
            </intent-filter>
        </service>

        <receiver
            android:name=".StartFirebaseAtBoot"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"></action>
            </intent-filter>
            <intent-filter>
                <action android:name="com.example.user.firebaseauth.RESTART_SERVICE"></action>
            </intent-filter>
        </receiver>

        <activity android:name=".MessageActivity"></activity>
    </application>

</manifest>